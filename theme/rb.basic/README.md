# theme/rb.basic

Production site theme (`cf_theme=rb.basic`).

- Member skins live in `skin/member/rb.member` (synced from `skin/member/onoff`).
- Core `head.php` / `tail.php` were restored from `theme/basic` after an FTP sync removed remote-only theme files.

Do not rely on server-only theme files outside git; keep this tree complete in the repository.
